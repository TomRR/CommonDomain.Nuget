using System.Text;
using Microsoft.CodeAnalysis;
using Microsoft.CodeAnalysis.Text;

namespace CommonDomain.Generators.InterfaceGenerators;

[Generator]
public class DateTimeProviderSourceGenerator : IIncrementalGenerator
{
    private const string Namespace = "Generators.Interfaces";

    private const string Name = "IDateTimeProvider";
    
    private const string SourceCode = $@"// <auto-generated/>
using System;

namespace {Namespace};

public interface IDateTimeProvider
{{
    DateTime UtcNow {{ get; }}
}}";

    public void Initialize(IncrementalGeneratorInitializationContext context)
    {
        // Add the marker attribute to the compilation.
        context.RegisterPostInitializationOutput(ctx => ctx.AddSource(
            $"{Name}.g.cs",
            SourceText.From(SourceCode, Encoding.UTF8)));
    }
}